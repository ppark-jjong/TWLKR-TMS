main 프로젝트 경로 = C:\MyMain\teckwah-dashboard\teckwah-TMS


프로젝트 설명서에 언급된 "중간 포인트마다 정상 작동 여부를 확인할 수 있는 로그를 간략히 적으세요"라는 요구사항에 따라 적절한 로깅을 추가해야 합니다. 다음 가이드라인을 따라 코드에 로그를 추가하세요:

1. 함수/메서드 진입점과 종료점
   - 중요 함수 시작 시: `logger.info(f"{함수명} 시작: 매개변수={간략한 매개변수}")`
   - 중요 함수 종료 시: `logger.info(f"{함수명} 완료: 결과={간략한 결과}")`

2. 데이터베이스 작업
   - 쿼리 실행 전: `logger.debug(f"DB 쿼리 시작: {쿼리 설명}")`
   - 쿼리 결과 후: `logger.debug(f"DB 쿼리 결과: {결과 요약}")`

3. 조건부 분기점
   - 중요 조건문 결정 시: `logger.debug(f"조건 평가: {조건}={결과}")`

4. 예외 처리
   - 예외 발생 시 상세 정보: `logger.error(f"{작업} 중 오류 발생: {오류 메시지}", exc_info=True)`

5. 중요 비즈니스 로직
   - 상태 변경: `logger.info(f"상태 변경: {대상} {이전 상태}->{새 상태}")`
   - 중요 계산: `logger.debug(f"{계산명} 계산: 입력={입력값}, 결과={결과값}")`

모든 로그는 간결하고 명확하게 작성하여 디버깅에 유용하되, 민감한 정보(비밀번호 등)는 포함하지 마세요.