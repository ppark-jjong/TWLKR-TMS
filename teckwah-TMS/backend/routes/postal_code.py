"""
우편번호 관련 라우트는 제거됨

설명:
- 우편번호 처리는 백엔드와 DB 레벨에서 자동 처리됨
- 프론트엔드에서 별도의 우편번호 API 호출이 불필요함
- 이 파일은 호환성 유지를 위해 남겨두었으나 실제 라우터는 삭제됨
"""

from fastapi import APIRouter

# 빈 라우터 (호환성 유지를 위해 파일만 남김)
router = APIRouter()
